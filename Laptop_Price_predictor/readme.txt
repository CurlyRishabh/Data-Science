check the model building folder to see how the model was build

The web app is created using streamlit, make sure to install the necessary package before running

You can run the main.py file using "streamlit run main.py" to see the web page

